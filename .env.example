PORT=8000
TRUEFORGE_URL=http://localhost:8790
# Optional: choose a configured TrueForge model FQN, e.g. provider/model.
TRUEFORGE_MODEL=
# Optional authorization for a hosted TrueForge instance. Never commit secrets.
TRUEFORGE_TOKEN=
# Local synthetic-data prototype only; server binds to loopback.
MCP_PUBLIC_URL=http://localhost:8000/mcp
